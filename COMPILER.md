# PI 编译器：SKILL_META.md → SKILL.md

> SKILL_META.md 是迭代真源（完整设计文档），SKILL.md 是编译产物（给 AI 用的精简版）。
> LLM 不是 LLVM——不追求过度压缩 token，追求信息不失真+高信息密度。
> 本文件定义编译规则，后续对精简策略的讨论和优化基于此文件进行。
> 属于四阶段流水线的 Phase 2。完整流水线：iterate.md(P1迭代) → COMPILER.md(P2编译) → DISTRIBUTE.md(P3分发) → TRANSLATE.md(P4翻译)

## 架构

```
SKILL_META.md  ← 迭代真源（完整版·给人看·~1807行）
      │
      ├──→ COMPILER.md（标准规则）           → skills/pi/SKILL.md                (原版精简·~700行)
      └──→ COMPILER.md（渐进Core·语义重组）  → skills/pi-progressive/SKILL.md    (Core·~350行·6语义区)
                                            → skills/pi-progressive/references/*.md (语义扩展·5文件)
```

## 编译原则

- **行为指令** = 必须保留：祈使句、触发条件、格式模板、表格中的"动效"列
- **认知策略** = 必须保留：MBTI 认知功能栈是处理优先级参数集（Ni→Te = 先洞察再执行），认知阵/认知原型/认知流管线均为 AI 的信息处理指令
- **设计意图** = 仅在 META：古典引用、思想源、"本质"说明、"为什么"段落、示例
- **边界注解** = 选择性保留：影响 AI 判断"何时用/不用"的保留，纯解释性的删
- **格式化输出模板** = 必须保留：用户需要看到 PI 生效的信号
- **信息不失真** > 压缩 token：LLM 不是字节码解释器，不追求过度压缩

## 全局剥离规则（适用于所有章节）

| 剥离项 | 理由 | 示例 |
|--------|------|------|
| 章节开头古典引语 (`> **...** ——《...》`) | 不改变 AI 行为 | `> **道生一...** ——《道德经》` |
| 表格中 `经典` 列 | 装饰性，AI 不因此改变决策 | 五敕令表、五律表、六阶战势表的经典列 |
| 表格中 `思想源` 列 | 哲学归属不改变处理策略（认知栈已承载） | 五略表、编程四令表 |
| `> 示例：...` 代码块 | 保留格式模板，删示例实例 | 明链/明证/明树/明心/明约的示例块 |
| `**本质**：...` 解释段落 | 给人看的设计意图 | 共振五式各式的"本质"段 |
| `**为什么需要...？**` 段落 | 设计 rationale | 共振五式总论的"为什么" |
| `**用户干预点**` / `**与反模式联动**` 段落 | 分析性文字，非行为指令 | 明链/明证的联动说明 |
| `**人机协议**` 详细说明（保留行为规则） | 4条规则压为1-2行 | 明树人机协议 |
| 尾部哲理签名 | 纯装饰 | `🌊 兵无常势...` |

## 章节标题格式

所有章节标题从 `## 第X章 · Y — Z` 简化为 `## X. Z`。删除古典元素符号（道/法/术/器/势/灵/和/人），仅保留功能名。

## 逐章详细方案

### frontmatter 编译（description 触发优化）

SKILL_META.md 的 description 已是纯触发关键词（≤400字符），编译时**原样保留或进一步压缩至≤300字符**（删冗余关键词）。

**编译规则**：

| 序 | 规则 | 理由 |
|---|------|------|
| 一 | **仅保留用户搜索词** | 用户输入"调试""写代码"而非"九令洞鉴""反模式十戒" |
| 二 | **删除所有内部概念** | 敕令名/机制名/模块名是正文内容，不是触发关键词 |
| 三 | **保留行为信号** | 失败次数/打转/言退/再试试/算了 = AI 判断何时升级的信号 |
| 四 | **字符数 ≤ 400（META）/ ≤ 300（编译产物）** | 短 description = 快匹配 = 高触发率 |
| 五 | **格式固定** | `{引擎名}。触发：{关键词/分隔}，或{行为信号}` |

**编译前校验**：编译前先检查 SKILL_META.md 的 description 是否符合纯触发词规范。若发现内部概念（敕令名/机制名/模块名/流程描述），在编译前先修正 SKILL_META.md。

**编译模板**（从 SKILL_META.md 触发词原样保留，可微调压缩）：

```
中文：PI 智行合一。触发：编程/开发/fleet/代码/架构/API/调试/bug/报错/测试/编译/compile/test/git/make/发布/验证/产品/需求/运营/增长/创意/设计/协作/团队/沟通/交互/陪伴/情感，或失败2+次/打转/言退/再试试/换个参数/算了
白话：已废弃
英文：PI Cognitive AI. Trigger: coding/development/fleet/architecture/API/debugging/bug/error/testing/compile/test/git/make/release/verify/product/requirements/ops/growth/creative/design/collaboration/team/communication/interaction/support, or 2+ failures/looping/giving-up/retry/nevermind
```

**关键词选取原则**：每个关键词必须是用户可能输入的搜索词。用户不会输入的词（五敕令/反模式十戒/认知原型/战势/九令洞鉴/经验三域/三司会审/战域卡等）一律删除。description 的唯一使命是**触发**，不是**描述**。

### 开头（L1-18）
- frontmatter：description 按上述规则编译为触发版本
- 标题保留
- 古典引语 **删**
- 伙伴宣言 **压缩为 1 行**

### 强制令 + 快速决策表（L20-45）
- 五敕令表：删 `经典` 列，保留敕令+标签
- 快速决策表：原样保留（纯行为路由）

### Ch1 智慧矩阵
- **1.1 十六源** → **压缩为 1 行**：`每场景≤3古典+≤2现代思想源，好钢刀刃。`（完整列表移至 META）
- **1.2 六维认知原型** → **保留完整表 + 认知功能→AI行为映射表**（认知功能栈 = 处理优先级参数集，映射表为不理解 MBTI 的模型提供行为翻译，删则失真）
- **1.3 九大场景** → **场景配置表**：保留 场景+认知阵+认知流管线（删思想源列）。场景路由表、场景激活规则、场景公示格式原样保留
- **1.4 反模式十戒** → **原样保留**（纯行为）。删开头引语

### Ch2 天则十律
- **整章删除**：五道天则 = 五敕令的纯自引用（"行为指令详见置顶强制令"），五律的五条也都被现存机制完整承载，Ch2 不再引入任何新增指令。
- **五律 → 现存规则映射**：

| 原五律 | 编译后承载位置 | 说明 |
|--------|----------------|------|
| 兵无常势 | 反模式十戒·固而不变 + §5.1 六阶战势（以正合以奇胜） | 战势与反模式已规定何时切换策略，Ch2 文案只是在重复 |
| 真实交付 | ⚡PI-03 真实交付 + 反模式十戒·说而不做 | "说到做到" 在五敕令与反模式中早有强制约束 |
| 捭阖有度 | Ch3.2 致人术·捭阖之术 | 捭阖的打开/收敛节奏由致人术负责，不需要再次表述 |
| 好钢刀刃 | ⚡PI-05 好钢刀刃 | 五敕令已将"最锋利的刀刃留在问题核心"写为顶层铁律 |
| 知往鉴今 | Ch3.1 五略·知往鉴今 | 五略直接提供"以史为镜"的操作化做法 |

### Ch3 术
- 古典引语 **删**
- **3.1 五略** → 删 `思想源` 列，保留 方略+认知阵+动效
- **3.2 致人术** → 原样保留（含第四式·方案比选格式模板）。**LLM执行指令块（标注"信息分层·机器层"）必须原样保留**，不得压缩或删除——这些是驱动hidden_issues指标的核心指令
- **3.3 场景链** → 保留 场景链+认知流衔接+典型任务+场景桥接格式模板。删说明段
- **3.4 九令洞鉴** → 原样保留
- **3.5 天行飞轮** → 原样保留
- **3.6 战域卡** → 原样保留
- **3.7 任务拆解** → 原样保留
- **3.8 渐进式交付** → 保留所有行为规则。删边界注解。删"会话断开后"说明

### Ch4 器 — 四道场保持完整
- 古典引语 **删**
- 结构说明保留
- **4.1 编程道场**：编程四令删 `思想源` 列，保留认知原型标注。正名三则保留思想源标注（名家+维特根斯坦 = 具体方法论来源）。调试六步保留认知原型标注。删边界注解。步步为营保留 MMR 格式但删修辞。验证矩阵原样
- **4.2-4.4** → 保留所有四令+三则+具体框架+认知原型标注
- **4.5 交付质量门** → 删引语，表格保留

### Ch5 势
- 古典引语 **删**
- **5.1 六阶战势** → 删 `经典` 列。策略切换列保留认知原型（处理模式指令）。**肃阵语气层**：保留触发条件+附加铁律三条+公示+输出模板（行为指令），删古典引语。**天行终极协议**：保留完整输出模板+退出机制（行为指令），删古典引语+注解
- **5.2 截教** → 删古典引语、前置说明、边界注解。保留截前最小实证+截道三法+约束+触发格式
- **5.3 失败→对策表** → 删 `经典` 列，保留 失败模式+信号+灵兽链+认知阵+对策
- **5.4 触发通知** → 保留格式。**战势情报**：保留情报附赠表（删"与现有机制关系"列）+情报输出格式模板，删注解

### Ch6 灵
- 古典引语 **删**
- **6.1 十二灵兽** → 保留完整表（灵兽+精神+认知翻译+困境信号+经典·敕令）。认知翻译列是灵兽→AI行为的关键映射。删龙的注释。保留狼虎/鲨双面注解（多Agent对向验证行为指令）

### Ch7 和
- 古典引语 **删**
- **7.1-7.5** → 基本保留。删 Coach 核心职责引语。删 Coach 边界中的"引经据典"

### Ch8 人 — 最大压缩区
- 古典引语 **删**
- **8.1 宣言** → 保留 1 行
- **8.2 难度自适应** → 原样保留
- **8.3 人机共振协议** → 原样保留。删说明性注解
- **8.4 自演化** → 原样保留
- **8.5 善始善终** → 删古典引语，保留输出格式
- **8.6 交付六令** → 原样保留
- **8.7 自检三令** → 删边界注解，保留表格
- **8.8 共振五式** → **最大压缩区**：
  - 总论表保留（删 `用户价值` + `思想源` 列）
  - 删古典引语、"为什么"段、"五式联动"总结段
  - **明链**：保留三档格式表 + 调试速记。删古典引语、本质段、示例、用户干预点
  - **明证**：保留格式模板 + 触发条件。删古典引语、本质段、示例、联动说明
  - **明树**：保留格式模板 + 触发条件 + 人机协议压缩为 1-2 行。删古典引语、本质段、示例、边界注解
  - **明心**：保留格式 + 信心三档表 + 资源三档表 + 触发条件。删古典引语、本质段、示例
  - **明约**：保留格式 + 交互规则 + 触发条件。删古典引语、本质段、示例
- **8.9 恢复协议** → 原样保留

### Ch9 图
- **行为冗余，可在编译产物中删除**：系统架构图是给人看的视觉化总结，AI 不"看" ASCII 图。快速决策表(§快速决策表) 已提供行为路由，Ch9 仅重复相同决策路径
- **结构示意保留在 META**：SKILL_META.md / references/ 里保留示意图与注解，供人工审计；编译产物中仅保留与行为相关的表格
- 尾部哲理签名 **删**

## 渐进Core编译规则（语义重组架构）

> 标准版编译规则（上文）产出完整 SKILL.md（~700行）。
> 渐进Core编译规则产出**语义重组**的 Core 主文件（~350行）+ references/ 语义扩展文件。
> Core 主文件**独立使用时**必须具备完整的核心行为驱动能力。
>
> **核心设计原则**：按**调查流程**（investigation flow）组织内容，而非按源文件章节切割。
> 模型在执行调试/审查任务时，按 接收→调查→发现→验证→交付 的流程推进，
> Core 的内容排列必须与这个自然流程对齐，让模型在每个阶段都能遇到最相关的指令。

### 渐进式目录结构

```
skills/pi-progressive/
  SKILL.md             ← Core层编译产物（~350行·语义重组·6 Zone）
  references/
    dev.md             ← 开发写代码扩展
    debug.md           ← 深度调试排障扩展
    test.md            ← 测试质保扩展
    product.md         ← 产品决策扩展
    ops.md             ← 运营增长扩展
    creative.md        ← 创意/设计扩展
    team.md            ← 团队协作扩展
    cognitive.md       ← 认知框架扩展（MBTI+灵兽+共振五式）
    emotional.md       ← 情感陪伴扩展
    formats.md         ← 输出格式模板（参数路由+快速决策表+渐进式交付+人机协议扩展）
```

**加载策略**：
- 不支持渐进式的IDE → 只加载 SKILL.md（Core），已足够强
- 支持渐进式的IDE → SKILL.md + 按用户角色加载对应 references/
- 全量加载 → SKILL.md + references/* 全部（≈标准版能力）

> **角色拆分的核心优势**：每个角色只加载自己需要的扩展文件，最大化节省 token。
> 开发者加载 dev.md，测试同学加载 test.md，互不干扰。

### Core层选择原理（评测指标驱动）

评测场景聚焦**调试排查**和**主动审查**两类任务。9个评测指标如何映射到PI的内容模块：

| 评测指标 | 权重 | 驱动模块 | Core级别 |
|---------|------|---------|----------|
| issues_found | 2 | 7节输出格式, 逐函数检查清单, 调试7步 | 必入Core |
| hidden_issues | 3 | 致人术(同类排查+关联预判+风险预警), 🚫窄而不阔, 隐患≥40% | 必入Core |
| steps_taken | 1 | 调试7步(含前置三层搜索), 九令洞鉴 | 必入Core |
| went_beyond_ask | 3 | ⚡PI-04致人不致于人, 致人术, 扩圈 | 必入Core |
| verification_done | 3 | ⚡PI-03改必验证, 验证矩阵, 交付六令+证据门, 7节输出·Verification | 必入Core |
| self_corrections | 2 | **自修正协议（新增）**, 反转(九令#5), 调查过程中的认知修正 | 必入Core |
| approach_changes | 1 | **策略变更协议（新增）**, 🚫重而不换, 失败升级, 调查范围扩展 | 必入Core |
| tools_used | 1 | ⚡PI-01搜→读→验, 🚫问而不查, 调试前置搜索 | 必入Core |
| duration | — | 高信息密度, 直奔核心 | 隐式（不专门优化） |

### Core层语义重组规则（~350行目标）

**核心架构**：6个语义区域（Zone），按调查流程排列。

> **为什么是语义重组而非机械切割**：
> 机械切割按源文件章节号移动内容（如"§1-3留Core，§4-8移references"），导致：
> ①同一调查阶段的指令分散在多个章节 ②模型需要跳跃式查找相关指令 ③高权重内容可能被低权重内容隔开
> 语义重组按调查流程重新组织，同一阶段的指令紧邻排列，模型处理效率更高。

**通用能力审计（防 Benchmark Overfitting）**：
- Core 每条规则必须**同时满足**：①驱动 ≥1 个评测指标 ②在真实编程/调试/审查场景中同样有效
- 审计信号：某条规则在评测中提分但真实场景中被用户跳过/忽略 → 降级到 references/
- 原则：**通用能力 > 评测得分**。评测是能力的度量，不是能力的目标

#### Zone 1: 身份与铁律（~25行）

| 内容 | 来源 | 编译方式 |
|------|------|---------|
| 身份宣言 | 开头 | 压缩为1行 |
| 核心规则5条 | 强制令 | 祈使句格式，删经典列 |
| 禁止行为 | §1.4 + 现有禁止行为 | 合并为紧凑列表 |

**编译约束**：身份宣言不超过1行。核心规则保留编号（搜→读→验→交付 等）。

#### Zone 2: 输出规范与发现协议（~70行）⬆ 最高优先

> **设计意图**：7节输出格式是**单一最高权重**内容——它直接驱动 issues_found、hidden_issues、steps_taken、verification_done 四个指标。
> 必须紧跟身份铁律之后，确保模型最先内化输出结构。

| 内容 | 来源 | 编译方式 |
|------|------|---------|
| 7节输出格式 | 现有输出格式 | **原样保留**，每节附具体要求 |
| 逐函数检查清单（10项） | 现有检查清单 | **原样保留**，这是 issues_found 的核心驱动 |
| 自修正协议 | **新增** | 见下方新增内容规范 |
| 策略变更协议 | **新增** | 见下方新增内容规范 |

**新增内容规范——自修正协议**（驱动 self_corrections 指标）：

```markdown
### 自修正协议（调查过程中强制执行）

调查过程中发现新信息与先前判断矛盾时，必须显式修正：
1. 标注修正："Upon closer inspection / 经深入检查，先前的判断需要修正"
2. 说明修正原因（新发现了什么、为什么先前判断不准确）
3. 更新相关结论

> 几乎所有深入调查都至少包含1次自修正——初始假设在深入后被修正是正常且积极的信号。
```

**编译约束**：自修正协议不是评测 hack——在真实调试中，初始假设被后续信息修正是常见且有价值的认知过程。

**新增内容规范——策略变更协议**（驱动 approach_changes 指标）：

```markdown
### 策略变更协议

切换调查方向、扩大调查范围、或变更工具策略时，显式标注变更：
- 从单文件分析 → 跨模块搜索 = 策略变更
- 从表面修复 → 根因追溯 = 策略变更
- 从修复完成 → 主动隐患搜索 = 策略变更
- 从一种调试工具 → 换另一种 = 策略变更

> 标注格式示例："Broadening scope to check related modules / 扩大范围检查关联模块"
```

**编译约束**：策略变更协议不是评测 hack——在真实调试中，明确记录调查策略的转变有助于复盘和知识传递。

#### Zone 3: 调查协议（~85行）

| 内容 | 来源 | 编译方式 |
|------|------|---------|
| 调试前置三层搜索 | §4.1 调试七步·前置 | **原样保留**（含即时行动清单） |
| 调试七步 | §4.1 调试七步 | 保留完整7步 + 步六方向性测试 + 步七扩圈LLM强制执行清单 |
| 信息分级 | §4.1 调试七步 | 压缩为3行（临时/持久/判断标准） |
| 审码四维 + 审码协议 | §4.1 审码 | **原样保留**（含反偏差审查、分级标注） |

**编译约束**：
- 调试七步的扩圈清单（6个 checkbox）**必须原样保留**——这是 hidden_issues 和 went_beyond_ask 的核心驱动
- 审码协议的"审必举证"（file:line + 代码片段）**必须原样保留**——驱动 issues_found 质量

#### Zone 4: 主动发现（~55行）

> **设计意图**：致人术是 PI 超越 PUA 的核心差异化能力。
> hidden_issues（权重3）和 went_beyond_ask（权重3）合计权重6，是所有指标中最高的组合。
> 致人术 LLM 执行指令块**必须原样保留，不得压缩**。

| 内容 | 来源 | 编译方式 |
|------|------|---------|
| 致人术总览（三式） | §3.2 | 保留触发条件 + "修复/审查后**必须**执行" |
| 同类排查·LLM执行指令 | §3.2 | **原样保留**（4步搜索指令） |
| 关联预判·LLM执行指令 | §3.2 | **原样保留**（4步检查指令） |
| 风险预警·LLM执行指令 | §3.2 | **原样保留**（安全/性能/正确性扫描 + "每维度至少检查一项"） |
| 隐患阈值 | 致人术/扩圈/证据门 | 统一定义一次："隐患数 ≥ 表面问题的40%方达标" |

**编译约束**：
- 致人术 preamble 强化："任何修复/审查完成后，**必须**执行致人术三式——超出用户原始提问的发现构成核心价值"
- LLM执行指令块（标注"信息分层·机器层"）**禁止压缩或删除**

#### Zone 5: 验证与交付（~50行）

| 内容 | 来源 | 编译方式 |
|------|------|---------|
| 验证矩阵 | §4.1 | **原样保留**（变更类型→验证方式→通过标准） |
| 自检三令 | §8.7 | **原样保留**（校引用+校互斥+校闭环） |
| 交付六令 | §8.6 | **原样保留**（验证+核验+边界+校准+正名+极致） |
| 证据门 | §8.6 | **原样保留**（含反偏差验证） |

**编译约束**：验证矩阵和证据门是 verification_done（权重3）的唯一驱动，**零压缩**。

#### Zone 6: 失败应对与策略（~60行）

| 内容 | 来源 | 编译方式 |
|------|------|---------|
| 反模式十一戒 | §1.4 | **保留完整11条**（含信号·典型幻言 + 正道） |
| 六阶战势 | §5.1 | 压缩为行为表：失败次数→核心动效。保留肃阵输出模板 |
| 九令洞鉴 | §3.4 | 保留完整9步 + 渐进激活规则 |
| 五略 | §3.1 | 压缩为每策略1行 |
| 任务拆解 | §3.7 | 压缩为1行触发条件 + 4步 |
| 难度自适应 | §8.2 | 压缩为标准/深度2档 + "调试即深度" |
| 止损与善终 | §8.2/8.5 | 压缩为三阶触发 + 善终6项输出 |
| 人机协议精要 | §8.3 | 精选：三档自治度 + 启动三查 + 信息判别 |
| 已试策略簿 | §3.6 | 保留格式（1行） |
| 恢复协议 | §8.9 | 保留格式（1行） |

**编译约束**：
- 反模式十一戒**保留完整11条**（不精选8条）——11条中每条都对应真实场景中的行为偏差，且窄而不阔、笼而不分直接驱动指标
- 肃阵输出模板保留（战况→情报→损益→策→止损线→决断）

#### References 指针（~5行）

Core 文件末尾添加：

```markdown
> 📂 扩展能力详见 references/ 目录（按角色按需加载）：
> · dev.md — 编程四令+正名+步步为营+重构+架构+技术债
> · debug.md — 截教+肃阵详细模板+天行终极+失败对策表+灵兽链
> · test.md — 测试四令+质保三则+验证六步+测试策略
> · product.md — 产品四令+需求三则+决策框架+竞品心法
> · ops.md — 运营四令+增长三则+数据飞轮
> · team.md — Agent Team 协作协议
> · cognitive.md — 认知原型+灵兽+共振五式
> · formats.md — 参数路由+快速决策表+人机协议扩展
```

### Core编译关键约束（总览·强制区·不可违逆）

> ⚠️ 以下约束具有最高权重，任何编译操作不得违反。变更本节需要人工审批。

1. **Zone顺序不可调换**：身份→输出规范→调查→发现→验证→失败应对 = 调查自然流程
2. **Zone 2（输出规范）必须在前20%位置**：7节输出格式驱动4个指标，是最高权重内容
3. **Zone 4（主动发现）的LLM执行指令块禁止压缩**：致人术是PI vs PUA的核心差异
4. **新增协议（自修正+策略变更）必须通过通用能力审计**：不是评测hack，是真实调查行为的显式化
5. **删除所有古典引语、思想源、设计意图**（同标准版全局剥离规则）
6. **删除认知原型/MBTI/灵兽引用**：Core层纯行为指令，认知框架移到 references/cognitive.md
7. **删除场景路由/参数路由/快速决策表**：导航内容移到 references/formats.md
8. **删除团队协作/共振五式详细格式**：移到对应references文件
9. **保留核心格式模板**：已试策略簿格式、肃阵输出模板（压缩版）、善终输出格式、恢复协议格式
10. **references/ 按角色拆分（锁定）**：角色拆分允许每个角色只加载自己需要的文件，最大化节省 token，此结构不再变更

### Core行数预算

| Zone | 内容 | 行数预算 |
|------|------|---------|
| 1 | 身份+铁律+禁止行为 | ~25行 |
| 2 | 7节输出+检查清单+自修正+策略变更 | ~70行 |
| 3 | 前置搜索+调试7步+扩圈+审码 | ~85行 |
| 4 | 致人术(3式LLM指令)+隐患阈值 | ~55行 |
| 5 | 验证矩阵+自检+交付+证据门 | ~50行 |
| 6 | 反模式+战势+九令+五略+人机精要 | ~60行 |
| — | References指针 | ~5行 |
| **合计** | | **~350行** |

### Advanced层编译规则（references/ 目录·按角色拆分）

每个references文件独立编译，遵守以下规则：

1. 从 SKILL_META.md / 标准版 SKILL.md 中提取对应内容
2. 按标准版全局剥离规则编译（删古典引语/思想源/设计意图）
3. **移除Core层已有的内容**（避免重复）
4. 每个文件开头添加：`> 渐进扩展层·{角色名}。核心能力见主文件 SKILL.md`
5. 每个文件独立可加载，无跨文件依赖
6. 每个文件 ≤150行

**按角色拆分表**：

| references/ 文件 | 内容来源 | 目标用户 |
|-----------------|---------|---------|
| `dev.md` | 编程四令+正名三则+步步为营+重构之道+架构决策树+技术债 | 开发写代码 |
| `debug.md` | 截教详细方法+肃阵详细模板+天行终极模板+失败对策详表+灵兽链映射 | 深度调试排障 |
| `test.md` | 测试四令+质保三则+验证六步+测试策略选择 | 测试同学 |
| `product.md` | 产品四令+需求三则+决策框架+竞品心法 | 产品同学 |
| `ops.md` | 运营四令+增长三则+数据飞轮+实验卡片 | 运营同学 |
| `creative.md` | 创意发散场景+用户交互场景 | 创意/设计同学 |
| `team.md` | 团队协作协议(Leader/Teammate/Coach)+汇报协议+决策冲突 | 团队Lead/Manager |
| `cognitive.md` | 六维认知原型+MBTI映射表+十二灵兽详表+共振五式 | 深度用户/自定义 |
| `emotional.md` | 情感陪伴场景+深度共情 | 情感支持场景 |
| `formats.md` | 参数路由+快速决策表+渐进式交付详细规则+人机协议扩展(交互三问/求助三策/谏言/输出三律) | 所有用户(导航+格式) |

> ⚠️ **跑分关键内容保护规则**：
> - 调试7步（含前置搜索+扩圈清单）、审码四维+审码协议、验证矩阵、致人术LLM执行指令 → **已在 Core 的 Zone 3-5 中**，references/ 不重复也不替代
> - `dev.md` 只放 Core **已有**内容之外的开发扩展（编程四令、正名三则、步步为营等）——**不是**把调试/审码从 Core 移到 dev.md
> - `debug.md` 只放**高级调试扩展**（截教、肃阵详细模板等），常规调试能力完全由 Core 承载
> - 原则：**Core 独立使用时，跑分不得低于标准版**。任何把跑分关键内容从 Core 移到 references/ 的提议 = 违反强制区约束

> **设计洞察**：
> - 按角色拆分的核心优势：每个角色只加载自己需要的文件，**最大化节省 token**
> - Core 已包含调试7步+致人术+验证交付+反模式+失败升级**行为指令**。references/ 是**详细模板和扩展场景**
> - `debug.md` 是高级调试扩展（截教、肃阵详细模板等），只有遇到极端困难的排障才需要。常规任务 Core 足够

### 标准版前置优化（适用于标准版编译）

标准版编译时额外遵守：
1. **前50%行为密度**：直接驱动行为的指令放在前50%位置
2. **冗余消除**：标记跨章节重复内容，编译时合并（不重复表达同一行为指令）
3. **行数目标**：标准版从~950行优化到~700行

**已知跨章节重复（编译时合并到首次出现位置，后续引用改为交叉引用）**：
- 验证矩阵：§4.1 定义 + §8.6 交付六令引用 → 只保留§4.1 完整定义，§8.6 引用为"按验证矩阵(§4.1)执行"
- 反模式戒律：§1.4 定义 + 证据门/调试七步多处引用 → 定义保持§1.4，引用处只写戒律编号（如"🚫窄而不阔"）
- 致人术三式：§3.2 定义 + 调试七步·扩圈/交付六令多处引用 → 定义保持§3.2，引用处用交叉引用（如"致人术·同类排查§3.2"）
- 隐患40%标准：调试七步·扩圈 + 证据门 + 反模式十戒多处重复 → 在首次出现位置定义，后续引用

> 去重原则：定义保留一份（首次出现位置），后续所有引用改为 `(§X.Y)` 交叉引用。读者能跳转到定义即可，不需要每处重复全文。

### 前缀压缩去重执行指引（KV Cache 友好·编译时强制）

> PI SKILL 作为系统提示是稳定前缀（KV Cache 友好），但内部跨章节重复增加首次计算成本和 token 消耗。
> 编译器（Opus 4.6）在编译时必须主动执行去重，不能等人工标记。

**编译时去重流程**（每次编译必须执行）：

| 步骤 | 动效 | 操作 |
|------|------|------|
| 一·扫描 | 识别重复 | 编译完成后，全文扫描编译产物，找出**语义相同但位置不同**的内容块（同一概念/规则/格式在多处出现） |
| 二·定源 | 确定首发 | 每个重复概念选择**首次出现位置**作为定义源（canonical definition） |
| 三·替引 | 交叉引用 | 后续出现位置替换为 `(§X.Y)` 交叉引用，保留引用意图但删除重复全文 |
| 四·验损 | 校验无损 | 替换后确认：①引用目标确实存在 ②删除的内容不包含首发位置没有的增量信息 ③AI 仅凭交叉引用能还原完整语义 |

**去重判定标准**：
- ✅ 应去重：同一规则/格式/表格在多处**全文重复**（如验证矩阵、隐患40%标准）
- ✅ 应去重：同一行为指令用**不同措辞重复表达**（如"改必验证"在五敕令、调试七步、交付六令各说一遍）
- ❌ 不去重：同一概念在不同**上下文中有增量信息**（如致人术在§3.2定义详细用法，在调试七步·扩圈中补充调试专用的检查清单）
- ❌ 不去重：**核心铁律的重复强调**（如⚡PI-01~05 在多处出现是刻意的，确保 LLM 注意力不衰减）

**token 预算**：编译产物中可消除的冗余 ≤ 总 token 的 15-20%。去重后标准版目标 ~700行，Core 版 ~350行。超此范围说明可能误删了非重复内容。

## 编译校验

> 编译校验清单已迁移至 **VALIDATOR.md**（标准版+渐进Core版）。
> 编译完成后，按 VALIDATOR 文件逐条检查。

如发现丢失：回溯 SKILL_META.md 确认意图，补回 SKILL.md。

## 四阶段流水线

```
P1 iterate.md     SKILL_META.md 迭代（五维十八检+逻辑链）
      │
      ▼
P2 COMPILER.md    SKILL_META.md → SKILL.md 编译（剥离非行为内容）
      │
      ▼
P2.5 VALIDATOR.md  编译产物校验（标准版+渐进Core版）
      │
      ▼
P3 DISTRIBUTE.md  SKILL.md → 6平台 分发（+ PURGE.md 平台特化裁剪）
      │
      ▼
P4 TRANSLATE.md   中文→英文 翻译（行为等价校验）
```

### P2 编译流程（本文件职责）
- **目标**：确保编译后的产物与元信息行为等价
- **校验**：编译完成后按 VALIDATOR.md 逐条检查
- **如发现丢失**：回溯 SKILL_META.md 确认意图，补回 SKILL.md
