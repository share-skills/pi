# PI 编译器：SKILL_META.md → SKILL.md

> SKILL_META.md 是迭代真源（完整设计文档），SKILL.md 是编译产物（给 AI 用的精简版）。
> LLM 不是 LLVM——不追求过度压缩 token，追求信息不失真+高信息密度。
> 本文件定义编译规则，后续对精简策略的讨论和优化基于此文件进行。
> 属于四阶段流水线的 Phase 2。完整流水线：iterate.md(P1迭代) → COMPILER.md(P2编译) → DISTRIBUTE.md(P3分发) → TRANSLATE.md(P4翻译)

## 架构

```
SKILL_META.md  ← 迭代真源（完整版·给人看·~1807行）
      │
      ├──→ COMPILER.md（标准规则）           → skills/pi/SKILL.md                (原版精简·~700行)
      └──→ tools/progressive.py（章节拆分）   → skills/pi-progressive/SKILL.md    (Core·~540行·4 references指针)
                                            → skills/pi-progressive/references/*.md (章节扩展·4文件)
```

## 编译原则

- **行为指令** = 必须保留：祈使句、触发条件、格式模板、表格中的"动效"列
- **认知策略** = 必须保留：MBTI 认知功能栈是处理优先级参数集（Ni→Te = 先洞察再执行），认知阵/认知原型/认知流管线均为 AI 的信息处理指令
- **设计意图** = 仅在 META：古典引用、思想源、"本质"说明、"为什么"段落、示例
- **边界注解** = 选择性保留：影响 AI 判断"何时用/不用"的保留，纯解释性的删
- **格式化输出模板** = 必须保留：用户需要看到 PI 生效的信号
- **信息不失真** > 压缩 token：LLM 不是字节码解释器，不追求过度压缩

## 全局剥离规则（适用于所有章节）

| 剥离项 | 理由 | 示例 |
|--------|------|------|
| 章节开头古典引语 (`> **...** ——《...》`) | 不改变 AI 行为 | `> **道生一...** ——《道德经》` |
| 表格中 `经典` 列 | 装饰性，AI 不因此改变决策 | 五敕令表、五律表、六阶战势表的经典列 |
| 表格中 `思想源` 列 | 哲学归属不改变处理策略（认知栈已承载） | 五略表、编程四令表 |
| `> 示例：...` 代码块 | 保留格式模板，删示例实例 | 明链/明证/明树/明心/明约的示例块 |
| `**本质**：...` 解释段落 | 给人看的设计意图 | 共振五式各式的"本质"段 |
| `**为什么需要...？**` 段落 | 设计 rationale | 共振五式总论的"为什么" |
| `**用户干预点**` / `**与反模式联动**` 段落 | 分析性文字，非行为指令 | 明链/明证的联动说明 |
| `**人机协议**` 详细说明（保留行为规则） | 4条规则压为1-2行 | 明树人机协议 |
| 尾部哲理签名 | 纯装饰 | `🌊 兵无常势...` |

## 章节标题格式

所有章节标题从 `## 第X章 · Y — Z` 简化为 `## X. Z`。删除古典元素符号（道/法/术/器/势/灵/和/人），仅保留功能名。

## 逐章详细方案

### frontmatter 编译（description 触发优化）

SKILL_META.md 的 description 已是纯触发关键词（≤400字符），编译时**原样保留或进一步压缩至≤300字符**（删冗余关键词）。

**编译规则**：

| 序 | 规则 | 理由 |
|---|------|------|
| 一 | **仅保留用户搜索词** | 用户输入"调试""写代码"而非"九令洞鉴""反模式十戒" |
| 二 | **删除所有内部概念** | 敕令名/机制名/模块名是正文内容，不是触发关键词 |
| 三 | **保留行为信号** | 失败次数/打转/言退/再试试/算了 = AI 判断何时升级的信号 |
| 四 | **字符数 ≤ 400（META）/ ≤ 300（编译产物）** | 短 description = 快匹配 = 高触发率 |
| 五 | **格式固定** | `{引擎名}。触发：{关键词/分隔}，或{行为信号}` |

**编译前校验**：编译前先检查 SKILL_META.md 的 description 是否符合纯触发词规范。若发现内部概念（敕令名/机制名/模块名/流程描述），在编译前先修正 SKILL_META.md。

**编译模板**（从 SKILL_META.md 触发词原样保留，可微调压缩）：

```
中文：PI 智行合一。触发：$pi/编程/开发/dev/code/代码/实现/架构/API/重构/调试/debug/bug/报错/异常/崩溃/超时/性能/优化/测试/test/编译/compile/git/make/发布/验证/审查/review/CR/产品/需求/运营/增长/创意/设计/协作/团队/沟通/交互/陪伴/情感，或深度/deep/失败2+次/反复失败/打转/卡住/言退/再试试/换个参数/算了
白话：已废弃
英文：PI Cognitive AI. Trigger: $pi/coding/dev/code/architecture/API/refactor/debug/bug/error/exception/crash/timeout/performance/optimization/test/compile/git/release/verify/review/CR/product/requirements/ops/growth/design/team/support, or deep/2+ failures/looping/stuck/giving-up/retry/nevermind
```

**metadata 固定要求**：标准版 frontmatter 必须保留 `metadata.version`、`metadata.homepage`、`metadata.copyright`、`metadata.argument-hint`。v23.1.1 起 `argument-hint` 固定为 `[loop|auto|wenyan] [场景名]`，Loop/Auto 管接续节奏，Wenyan/文言只管输出。

**关键词选取原则**：每个关键词必须是用户可能输入的搜索词。用户不会输入的词（五敕令/反模式十戒/认知原型/战势/九令洞鉴/经验三域/三司会审/战域卡等）一律删除。description 的唯一使命是**触发**，不是**描述**。

### 开头（L1-18）
- frontmatter：description 按上述规则编译为触发版本
- frontmatter metadata：保留版本、主页、版权、`argument-hint: "[loop|auto|wenyan] [场景名]"`
- 标题保留
- 古典引语 **删**
- 伙伴宣言 **压缩为 1 行**

### 强制令 + 快速决策表（L20-45）
- 五敕令表：删 `经典` 列，保留敕令+标签
- 快速决策表：原样保留（纯行为路由）

### Ch1 智慧矩阵
- **1.1 十六源** → **压缩为 1 行**：`每场景≤3古典+≤2现代思想源，好钢刀刃。`（完整列表移至 META）
- **1.2 六维认知原型** → **保留完整表 + 认知功能→AI行为映射表**（认知功能栈 = 处理优先级参数集，映射表为不理解 MBTI 的模型提供行为翻译，删则失真）
- **1.3 九大场景** → **场景配置表**：保留 场景+认知阵+认知流管线（删思想源列）。场景路由表、场景激活规则、场景公示格式原样保留
- **1.4 反模式十戒** → **原样保留**（纯行为）。删开头引语

### Ch2 天则十律
- **整章删除**：五道天则 = 五敕令的纯自引用（"行为指令详见置顶强制令"），五律的五条也都被现存机制完整承载，Ch2 不再引入任何新增指令。
- **五律 → 现存规则映射**：

| 原五律 | 编译后承载位置 | 说明 |
|--------|----------------|------|
| 兵无常势 | 反模式十戒·固而不变 + §5.1 六阶战势（以正合以奇胜） | 战势与反模式已规定何时切换策略，Ch2 文案只是在重复 |
| 真实交付 | ⚡PI-03 真实交付 + 反模式十戒·说而不做 | "说到做到" 在五敕令与反模式中早有强制约束 |
| 捭阖有度 | Ch3.2 致人术·捭阖之术 | 捭阖的打开/收敛节奏由致人术负责，不需要再次表述 |
| 好钢刀刃 | ⚡PI-05 好钢刀刃 | 五敕令已将"最锋利的刀刃留在问题核心"写为顶层铁律 |
| 知往鉴今 | Ch3.1 五略·知往鉴今 | 五略直接提供"以史为镜"的操作化做法 |

### Ch3 术
- 古典引语 **删**
- **3.1 五略** → 删 `思想源` 列，保留 方略+认知阵+动效
- **3.2 致人术** → 原样保留（含第四式·方案比选格式模板）。**LLM执行指令块（标注"信息分层·机器层"）必须原样保留**，不得压缩或删除——这些是驱动hidden_issues指标的核心指令
- **3.3 场景链** → 保留 场景链+认知流衔接+典型任务+场景桥接格式模板。删说明段
- **3.4 九令洞鉴** → 原样保留
- **3.5 天行飞轮** → 原样保留
- **3.6 战域卡** → 原样保留
- **3.7 任务拆解** → 原样保留
- **3.8 渐进式交付** → 保留所有行为规则。删边界注解。删"会话断开后"说明。v23.1 起必须保留"Loop强接续/Auto可收束"；v23.1.1 起文言只改输出，不取消三段式语义

### Ch4 器 — 四道场保持完整
- 古典引语 **删**
- 结构说明保留
- **4.1 编程道场**：编程四令删 `思想源` 列，保留认知原型标注。正名三则保留思想源标注（名家+维特根斯坦 = 具体方法论来源）。**实现复用门必须保留**（写/改非平凡代码前先搜现有能力、贴本地模式、复用不足再抽象、改动收口）。调试六步保留认知原型标注。删边界注解。步步为营保留 MMR 格式但删修辞。验证矩阵原样
- **4.2-4.4** → 保留所有四令+三则+具体框架+认知原型标注
- **4.5 交付质量门** → 删引语，表格保留

### Ch5 势
- 古典引语 **删**
- **5.1 六阶战势** → 删 `经典` 列。策略切换列保留认知原型（处理模式指令）。**肃阵语气层**：保留触发条件+附加铁律三条+公示+输出模板（行为指令），删古典引语。**天行终极协议**：保留完整输出模板+退出机制（行为指令），删古典引语+注解
- **5.2 截教** → 删古典引语、前置说明、边界注解。保留截前最小实证+截道三法+约束+触发格式
- **5.3 失败→对策表** → 删 `经典` 列，保留 失败模式+信号+灵兽链+认知阵+对策
- **5.4 触发通知** → 保留格式。**战势情报**：保留情报附赠表（删"与现有机制关系"列）+情报输出格式模板，删注解

### Ch6 灵
- 古典引语 **删**
- **6.1 十二灵兽** → 保留完整表（灵兽+精神+认知翻译+困境信号+经典·敕令）。认知翻译列是灵兽→AI行为的关键映射。删龙的注释。保留狼虎/鲨双面注解（多Agent对向验证行为指令）

### Ch7 和
- 古典引语 **删**
- **7.1-7.5** → 基本保留。删 Coach 核心职责引语。删 Coach 边界中的"引经据典"

### Ch8 人 — 最大压缩区
- 古典引语 **删**
- **8.1 宣言** → 保留 1 行
- **8.2 难度自适应** → 原样保留。交互/输出模式必须包含 Loop、Auto、文言；Loop/Auto 是接续节奏，文言只管输出，可与场景和难度叠加，且不得省略证据、验证、file:line
- **8.3 人机共振协议** → 原样保留。删说明性注解
- **8.4 自演化** → 原样保留
- **8.5 善始善终** → 删古典引语，保留输出格式
- **8.6 交付六令** → 原样保留
- **8.7 自检三令** → 删边界注解，保留表格
- **8.8 共振五式** → **标准版最大压缩区 / 渐进Core版保留格式模板**：
  - 总论表保留（删 `用户价值` + `思想源` 列）
  - 删古典引语、"为什么"段、"五式联动"总结段
  - **明链**：保留三档格式表 + 调试速记。删古典引语、本质段、示例、用户干预点
  - **明证**：保留格式模板 + 触发条件。删古典引语、本质段、示例、联动说明
  - **明树**：保留格式模板 + 触发条件 + 人机协议压缩为 1-2 行。删古典引语、本质段、示例、边界注解
  - **明心**：保留格式 + 信心三档表 + 资源三档表 + 触发条件。删古典引语、本质段、示例
  - **明约**：保留格式 + 交互规则 + 触发条件。删古典引语、本质段、示例
  - **渐进Core版**：上述格式模板必须内联在 Core 末尾（references 指针前），详细触发说明可留在 references/resonance-forms.md
- **8.9 恢复协议** → 原样保留

### Ch9 图
- **行为冗余，可在编译产物中删除**：系统架构图是给人看的视觉化总结，AI 不"看" ASCII 图。快速决策表(§快速决策表) 已提供行为路由，Ch9 仅重复相同决策路径
- **结构示意保留在 META**：SKILL_META.md / references/ 里保留示意图与注解，供人工审计；编译产物中仅保留与行为相关的表格
- 尾部哲理签名 **删**

## 渐进Core编译规则（章节拆分架构）

> 标准版编译规则（上文）产出完整 SKILL.md（~700行）。
> 渐进Core编译规则产出**章节拆分**的 Core 主文件（~540行）+ 4 个 references/ 章节扩展文件。
> Core 主文件**独立使用时**必须具备身份、路由、致人术、人机协议、交付与恢复等高频行为驱动能力。
>
> **当前生成器原则**：`tools/progressive.py` 从标准版 SKILL.md 做章节拆分，Core 保留 Ch1-3、Ch8 主体与恢复协议；Ch4、Ch5-6、Ch7、§8.8 细节拆入 4 个 references 文件。

### 渐进式目录结构

```
skills/pi-progressive/
  SKILL.md             ← Core层编译产物（~540行·章节拆分+4 references 指针）
  references/
    four-dojos.md        ← Ch4 四道场（编程/测试/产品/运营 + 审码五维）
    battle-momentum.md   ← Ch5-6 战势 + 灵兽
    team-protocol.md     ← Ch7 团队协作
    resonance-forms.md   ← §8.8 共振五式详细格式
```

**加载策略**：
- 不支持渐进式的IDE → 只加载 SKILL.md（Core），已足够强
- 支持渐进式的IDE → SKILL.md + 按需加载对应 references/
- 全量加载 → SKILL.md + references/* 全部（≈标准版能力）

> **章节拆分的核心优势**：Core 快速加载高频规则，低频长章节按需加载；references 文件数量固定为 4，避免旧分片残留。

### 渐进式章节拆分规则

| 输出文件 | 来源范围 | 生成方式 |
|---------|----------|---------|
| Core `SKILL.md` | 标准版开头→Ch4 前 + Ch8→§8.8 前 + §8.9→结尾 | 保留原文，插入 4 个 reference 摘要与链接 |
| `references/four-dojos.md` | Ch4 四道合一 | 去除开头分隔线，保留四道场完整行为规则 |
| `references/battle-momentum.md` | Ch5 动态响应 + Ch6 灵兽图腾 | 去除开头分隔线，保留战势/截教/灵兽 |
| `references/team-protocol.md` | Ch7 团队协作 | 去除开头分隔线，保留团队协议 |
| `references/resonance-forms.md` | §8.8 共振五式 | 去除开头分隔线，保留详细格式与触发 |

**强制约束**：
1. references 写入前必须清空旧目录，避免旧分片残留。
2. Core 指针只列 4 个当前 references 文件。
3. Core + references 总内容覆盖标准版 body ≥ 95%。
4. 支持 references/ 的平台用 `tools/distribute.py <source> prog|prog-en` 分发；不支持 references/ 的平台继续使用完整单文件标准版。

Core 文件插入的 references 指针：

```markdown
> 📂 扩展能力详见 references/ 目录（按角色按需加载）：
> · four-dojos.md — 编程/测试/产品/运营四大道场 + 审码五维 + 验证矩阵
> · battle-momentum.md — 六阶战势 + 肃阵 + 天行终极 + 截教 + 灵兽
> · team-protocol.md — Agent Team 协作协议 + 决策三权 + Coach巡检
> · resonance-forms.md — 共振五式详细格式与触发说明
```

### 标准版前置优化（适用于标准版编译）

标准版编译时额外遵守：
1. **前50%行为密度**：直接驱动行为的指令放在前50%位置
2. **冗余消除**：标记跨章节重复内容，编译时合并（不重复表达同一行为指令）
3. **行数目标**：标准版从~950行优化到~700行

**已知跨章节重复（编译时合并到首次出现位置，后续引用改为交叉引用）**：
- 验证矩阵：§4.1 定义 + §8.6 交付六令引用 → 只保留§4.1 完整定义，§8.6 引用为"按验证矩阵(§4.1)执行"
- 反模式戒律：§1.4 定义 + 证据门/调试七步多处引用 → 定义保持§1.4，引用处只写戒律编号（如"🚫窄而不阔"）
- 致人术三式：§3.2 定义 + 调试七步·扩圈/交付六令多处引用 → 定义保持§3.2，引用处用交叉引用（如"致人术·同类排查§3.2"）
- 隐患40%标准：调试七步·扩圈 + 证据门 + 反模式十戒多处重复 → 在首次出现位置定义，后续引用

> 去重原则：定义保留一份（首次出现位置），后续所有引用改为 `(§X.Y)` 交叉引用。读者能跳转到定义即可，不需要每处重复全文。

### 前缀压缩去重执行指引（KV Cache 友好·编译时强制）

> PI SKILL 作为系统提示是稳定前缀（KV Cache 友好），但内部跨章节重复增加首次计算成本和 token 消耗。
> 编译器（Opus 4.6）在编译时必须主动执行去重，不能等人工标记。

**编译时去重流程**（每次编译必须执行）：

| 步骤 | 动效 | 操作 |
|------|------|------|
| 一·扫描 | 识别重复 | 编译完成后，全文扫描编译产物，找出**语义相同但位置不同**的内容块（同一概念/规则/格式在多处出现） |
| 二·定源 | 确定首发 | 每个重复概念选择**首次出现位置**作为定义源（canonical definition） |
| 三·替引 | 交叉引用 | 后续出现位置替换为 `(§X.Y)` 交叉引用，保留引用意图但删除重复全文 |
| 四·验损 | 校验无损 | 替换后确认：①引用目标确实存在 ②删除的内容不包含首发位置没有的增量信息 ③AI 仅凭交叉引用能还原完整语义 |

**去重判定标准**：
- ✅ 应去重：同一规则/格式/表格在多处**全文重复**（如验证矩阵、隐患40%标准）
- ✅ 应去重：同一行为指令用**不同措辞重复表达**（如"改必验证"在五敕令、调试七步、交付六令各说一遍）
- ❌ 不去重：同一概念在不同**上下文中有增量信息**（如致人术在§3.2定义详细用法，在调试七步·扩圈中补充调试专用的检查清单）
- ❌ 不去重：**核心铁律的重复强调**（如⚡PI-01~05 在多处出现是刻意的，确保 LLM 注意力不衰减）

**token 预算**：编译产物中可消除的冗余 ≤ 总 token 的 15-20%。去重后标准版目标 ~700行，Core 版目标 ~540行。超此范围说明可能误删了非重复内容。

## 编译校验

> 编译校验清单已迁移至 **VALIDATOR.md**（标准版+渐进Core版）。
> 编译完成后，按 VALIDATOR 文件逐条检查。

如发现丢失：回溯 SKILL_META.md 确认意图，补回 SKILL.md。

## 四阶段流水线

```
P1 iterate.md     SKILL_META.md 迭代（五维十八检+逻辑链）
      │
      ▼
P2 COMPILER.md    SKILL_META.md → SKILL.md 编译（剥离非行为内容）
      │
      ▼
P2.5 VALIDATOR.md  编译产物校验（标准版+渐进Core版）
      │
      ▼
P3 DISTRIBUTE.md  SKILL.md → 6平台 分发（+ PURGE.md 平台特化裁剪）
      │
      ▼
P4 TRANSLATE.md   中文→英文 翻译（行为等价校验）
```

### P2 编译流程（本文件职责）
- **目标**：确保编译后的产物与元信息行为等价
- **校验**：编译完成后按 VALIDATOR.md 逐条检查
- **如发现丢失**：回溯 SKILL_META.md 确认意图，补回 SKILL.md

## Eval 驱动编译优化

### 编译后回归验证

编译产物必须通过 benchmark/eval_criteria.md 定义的全部标准。编译→验证→修正 闭环：

1. 编译 SKILL.md
2. 跑 `python3 benchmark/local_run.py --backend qodercli`
3. 对照 eval_criteria.md 逐项检查
4. 未达标项 → 回溯 COMPILER.md 规则 → 修正 → 重编译
5. 每轮结果存储到 benchmark/iteration_logs/{timestamp}.md

### 场景覆盖验证

编译产物中必须包含以下场景类型的调试指令：

| 场景类型 | 必备指令 | 对应 Eval 场景 |
|---------|---------|--------------|
| 代码层 | import/语法/逻辑/性能调试 | S1,S2,S5 |
| 基础设施层 | Docker/端口/连接/配置/维度匹配 | S3 |
| 数据层 | 格式/编码/维度/Schema | S6 |
| API层 | 契约/兼容性/认证 | S4 |
| 审查审计 | 逐项举证+审计验证标准+验证完整性自检 | S7,S8,S9 |
| 自修正行为 | 双层自修正（假设修正+范围/深度修正）+显式修正标记 | 全场景 |
| 策略变更行为 | 阶段转换标注（读→搜→验→扩各为策略变更）+ 方向切换标注 | 全场景 |
| 工具多样性 | ≥3种工具类型（搜→读→验链路） | 全场景 |
