#!/bin/bash
# PI 智行合一引擎 — PreCompact Hook
# 在上下文压缩前注入恢复协议提醒，确保 PI 状态被保存
#
# 对应 PI §8.9 上下文恢复协议：
#   会话续接或压缩后：🔍查势（失败计数+战阶）→ 🎯查域（场景+认知阵）→ 📝查簿（已试策略簿）

cat << 'EOF'
{
  "systemMessage": "⚠️ PI 上下文恢复协议（PreCompact 自动触发）：上下文即将压缩。请在压缩前输出恢复快照，格式：\n\n🔄 PI · 恢复 · {当前场景} · 败{N} · {当前战阶} · 已排{M}策\n\n包含：当前场景、失败计数、战阶、已试策略簿、关键决策、当前任务状态。压缩后将基于此快照恢复 PI 引擎状态。"
}
EOF
